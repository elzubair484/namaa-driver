import 'dart:async';
import 'package:flutter/material.dart';
import 'package:go_router/go_router.dart';
import '../route_names.dart';

FutureOr<String?> verificationGuard(BuildContext context, GoRouterState state) {
  // Full logic handled in routerProvider redirect. This is kept for per-route use.
  return null;
}
